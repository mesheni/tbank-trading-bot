pandas>=2.2
numpy>=1.26
statsmodels>=0.14
lightgbm>=4.3
scikit-learn>=1.4
requests>=2.31
python-dotenv>=1.0
joblib>=1.3
pytest>=8.0

# --- Опционально: нейросетевой NLP-контур (эмбеддинги + трансформер тональности).
# Без них работает лексиконный фолбэк тональности и кластеризация отключается.
# sentence-transformers>=3.0
# transformers>=4.40
# torch --index-url https://download.pytorch.org/whl/cpu

# --- Опционально: официальный gRPC SDK (требует Python <= 3.12, в проекте не обязателен):
# tinkoff-investments
